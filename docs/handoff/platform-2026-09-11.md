# Handoff — platform lane (Ali), end of 11 Sep

Read `CLAUDE.md` first; it is the shared contract and it is current. This file
is only the things a fresh session cannot infer from the repo.

---

## Where the work is

**Branch `feat/plat-day2-groundwork`, 4 commits ahead of `main`, NOT pushed.**

```
77ea1d1  Fix what review and the merged-tree audit found in the platform lane
913ec08  Close the hard-rule-4 gap: nothing could record a signature
ef80b37  Groundwork: safe reset guard, the table script, the real Python floor
5aa1c00  Fix what the review found in today's spine
```

It is rebased on top of `feat/plat-spine-review-fixes` (which IS pushed but
never merged), so those two are one line of work now — do not merge the older
branch separately.

**81 passed, 1 skipped, ruff clean** on every file this lane owns.

Ali's standing instruction: **do not push platform work.** Report, review
together, then decide. Keep doing that unless he says otherwise.

---

## The rules Ali set that are easy to violate

1. **Never edit another lane's files.** Findings in Kartik's, Alakshendra's or
   Raghav's code go into a review doc on a branch cut from *their* tip
   (`review/<lane>`), so `git merge origin/review/<lane>` drops it into their
   tree with no conflict. Three of those are already pushed.
   Platform files are: `graph/`, `app.py`, `agents/digest.py`, `scripts/`,
   `docs/`, and the shared infra Ali created — `core/db.py`, `core/memstore.py`,
   `core/fakes.py`, `tests/conftest.py`.
2. **`/code-review` every branch before asking a human to look.** It has found
   real bugs in this lane's own code twice, including twice in code written
   minutes earlier.
3. **Reproduce before reporting.** Every finding in every review doc was
   executed, not inferred. One finding from a teammate's review did not
   reproduce as described and the test that "confirmed" it was wrong — verify
   your own repro too.
4. **Do not push.** See above.

---

## Merge queue

| Branch | State |
|---|---|
| `feat/plat-day2-groundwork` (this one) | local only, green |
| `feat/plat-spine-review-fixes` | pushed, superseded — folded into the above |
| `alakshendra/ladder-and-filing-client` | **held**, 2 blockers, review pushed |
| `feat/mesh-day1` (Kartik) | 1 blocker his + 1 mine, review pushed |
| `feat/household-time` (Raghav) | 1 blocker, review pushed; he has it fixed locally, unpushed (needs `--force-with-lease`, it is a rebase) |
| `alakshendra/institutions-day1` | superseded, should be deleted |

All four merge cleanly into `main` individually. A cumulative merge conflicts
only on `STATUS.md`, twice, both sides appended — hand-resolve, no code
conflicts anywhere.

`STATUS.md` has forked into four versions with no shared timestamp. Needs a
reconciliation pass before anyone trusts it at merge time. **Not done.**

---

## Open decisions that need the group, not one person

**1. A version guard on `Case`.** Three writers mutate a case — the request
Graph, Kartik's ambient `apply_upgrade()`, Raghav's Watchdog `climb()` — and
`put_case` is a blind overwrite everywhere. This surfaced independently in all
three reviews. `_open_or_load_case` in `graph/request_path.py` is documented as
check-then-act and deliberately left open: three people inventing three
mechanisms is the failure mode. **One conditional-write design, agreed once.**

**2. TAU across the regime change** (Kartik's finding, and the sharpest one
anyone made). The renormalised path reaches 1.0 on topology and recency alone;
the full three-term path needs `semantic >= 0.2` just to clear TAU 0.72. So
turning embeddings on *stops* pairs clustering that cluster today. Ali's
position: two thresholds, `TAU_TOPOLOGICAL` and `TAU_FULL`, swept
independently, with the trace naming which one applied. Not ratified.

**3. `core/tags.py` ownership.** Alakshendra created it outside his lane and
two lanes import it. Ali said he would take it into platform so the shape
question sits with the trace UI. Not yet moved.

---

## What Ali answered today, so nobody re-litigates it

**Signing: every tier needs a fresh signature.** Tier 1 is a complaint; tier 3
is a statutory appeal that can dock an officer's pay under Sakala; tier 4 needs
a name, address and Rs 10. Consent for the first is not informed consent for
the fourth. It was not answerable before today because **nothing in the repo
could record a signature at all** — `signed_by` was read by the decoder,
required by the institution client, and written by nobody.

That is now closed in this lane: `db.sign_filing()` + `digest.approve(key,
member_id, clock)`. First signature wins; an empty `member_id` is refused.
`core/store.py` still needs a DynamoDB implementation of `sign_filing`,
`unsigned_filings`, `get_filing` — they are in `db.OPTIONAL`, so the seam
raises by name instead of binding None.

**The `DeskReply` -> `Callable[[Filing], bool]` adapter belongs in
`institutions/client.py`**, not the Watchdog — the temporal lane must not
import the institutions lane. The collapse is `reply.filed`, **not**
`should_pause_sla is False`: those diverge on REJECTED, which did not land and
must not be retried, and `climb()` retries twice on a falsey submit.

**Alakshendra's `compose_filing()` wins over Raghav's `_draft_filing()`** — it
builds from curated `required_fields` and refuses with `missing_fields` rather
than filing something incomplete. Raghav's to delete, not ours.

---

## Known defects raised for other lanes, deliberately not fixed here

- **`agents/remedy.lookup()` hands out mutable references** from a module-level
  cache, so any caller mutating an entry corrupts the table for the whole
  process. Found when a platform test did `entry.ladder = []` and broke 7 of
  Raghav's Watchdog tests in a merged tree. The test now deep-copies; the cache
  is Alakshendra's to fix.
- Kartik's `feat/mesh-day1` is 8 commits behind `main` and will conflict on
  `core/clock.py`, `tests/conftest.py`, `tests/test_contract.py`, `CLAUDE.md`,
  `STATUS.md`. Needs a rebase before it grows.
- `AWS_PROFILE=""` fabricates a failure inside strands' Agent construction —
  botocore rejects a profile literally named `''`. Use `env -u AWS_PROFILE` to
  prove the suite needs no credentials.
- 5 Windows temp-dir permission errors under OneDrive in the institutions and
  routing tests. Environmental, shows red on any Windows machine.

---

## Platform work NOT started

In Ali's brief, in rough priority:

1. **AgentCore deploy.** AgentCore is live and was never blocked — only the
   model data plane is. `app.py` now has one entrypoint (two `@app.entrypoint`
   decorators silently overwrite each other; `health` was shadowing `invoke`,
   which would have made the whole spine unreachable once deployed). The brief
   is blunt: broken on Tuesday costs an hour, broken on Thursday costs the
   project.
2. **Observability** — OTEL with `aws-opentelemetry-distro`, every span tagged
   `case_id`. Do this before the trace UI; the grouping is what makes the UI
   possible.
3. **Trace UI.** `graph/trace.py` is the surface and already carries
   `CaseTrace`, `Transition`, and a ContextVar-bound `record()` any lane can
   call without threading the object through. That last piece exists because
   three trace formats had grown in parallel.
4. **The builder.aws blog post** — +0.6 on a five-criterion scale, and always
   the thing that slips. Write it while the decisions are fresh.
5. **Video** *(moved to Raghav, 14 Sep)*. Three moments: grounded routing with the citation visible, the
   ambient upgrade, the disputed closure. Plus a real ten-minute `RealClock`
   trace beside the compressed one — that pairing is the whole rebuttal to
   "you faked the hard part".

---

## Bedrock

Still `ValidationException: Operation not allowed` on every invoke, all
vendors, all regions. Account-level, support case 178898467100367, nothing in
the console fixes it. **AgentCore itself is live** — the deploy target was
never blocked.

`core/models.py` is the seam: `PANCHAYAT_MODEL=anthropic` plus an
`ANTHROPIC_API_KEY` swaps provider in one env var if the account never clears.
Ali should re-read the hackathon rules for whether Bedrock is required as the
*model* provider or just AWS/AgentCore as the platform — that decides whether
this is the shipping config or a dev-time bridge.
