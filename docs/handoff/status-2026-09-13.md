# Status — 13 Sep, after the deploy

Short answer to "is everything correct now": **the deployed system works and is
verified. The project is not finished.** Nine things below are genuinely
outstanding, and two of them decide whether the demo shows the product or
shows a router.

Everything here was checked by running or grepping it today, not recalled.

---

## What is live

```
arn:aws:bedrock-agentcore:ap-south-2:699073937307:runtime/panchayat-3FFhtr5OfG
```

**Verified end to end against the real DynamoDB table in ap-south-2**, not from
a test:

| | |
|---|---|
| A household reports | routes to BWSSB with citation, drafts to the Assistant Engineer, tier 1, SLA set |
| The table | 5 rows written — claim, filing, filing pointer, feeder, case |
| `list_cases` | reads the case back for that household |
| `get_case` | draft body visible, `yours: true`, no household id in the response |
| `approve` | records `mem_001` |
| `approve` twice | refused, original signatory intact |

**Suite: 449 passed, 37 skipped, `ruff` clean.**

Hard rule 4 — agents draft, humans sign — now works on a deployed system for
the first time. That was **P0-2** and it is closed.

### Why ap-south-2

`maxAgents` is **0 in us-east-1, us-west-2, eu-west-1 and ap-south-1**, and at
the full default of 1,000 in ap-south-2. The quota is per-region. Four zeroed
regions were nearly generalised to "all of them", which would have left us
waiting on a support ticket that did not need to exist.

Side effect worth keeping: Hyderabad is in India, so the table and the runtime
are both in-country. The model calls are not, and never were.

---

## What is NOT correct yet

### P0 — the demo is weaker without these

**1. Nothing schedules `check_closure`.** Grepped every `clock.schedule()`
call: zero hits. A desk returning `CLOSED` triggers nothing, so the
disputed-closure moment — **the peak of the video** — can still only fire from
tests and Kartik's harness. `reconcile_closure` is correct; it just never
runs. **This needs a group decision on the trigger, not AWS.** It is the single
highest-value thing left.

**2. The temporal path is not deployed.** `handlers/temporal.py` exists and is
tested; no Lambda runs it, and no EventBridge schedule exists. So no case ever
breaches its SLA or escalates in production. `core/clock.py` raises
`SchedulerNotConfigured` until `WATCHDOG_LAMBDA_ARN` and `SCHEDULER_ROLE_ARN`
are set. Policies are written (`docs/deploy/scheduler-role-*.json`); the Lambda
is not packaged.

### P1 — real gaps, visible to a judge who asks

**3. There is no authentication.** Anyone who can invoke the endpoint can read
any case by id and approve any filing by passing any `member_id` string. Hard
rule 4's "the liability lands on a named person" is currently only as strong as
the caller's honesty. **The frontend team must know this**: a UI that lets
someone type their own `member_id` makes the signature meaningless.

**4. The ambient path has no entry point.** `handlers/ambient.py` does not
exist. Streams are enabled on the table and nothing consumes them, so
clustering never runs in production. `handlers/temporal.py` is the shape to
copy.

**5. A cloud runtime cannot reach `localhost` institution desks.**
`institutions/server.py` runs on ports 9001–9005 on someone's laptop. The
deployed agent cannot call them. Either demo the end-to-end filing locally, or
give the desks public endpoints and set `WARD_ENDPOINT` and friends at deploy.
**Decide before recording, not during.**

**6. Bedrock models are still blocked account-wide** —
`ValidationException: Operation not allowed`, re-probed today in us-east-1 and
us-west-2. Support case 178898467100367. The spine runs anyway because its
nodes take an injected model and fall back to deterministic parsing, measured
at `totalTokens: 0`. It costs quality, not availability.

**7. The trace UI does not exist.** `demo/trace_ui/` is an empty untracked
folder. The data is all there — spans carry `case_id`, and AgentCore
instruments the runtime — but nothing renders it.

### P2 — smaller, known, unowned

**8. `list_cases` shows only open cases.** `open_cases()` excludes RESOLVED,
WITHDRAWN and DORMANT, so "your complaint was resolved" — the most satisfying
screen in the product — cannot be built. Needs a by-household query on
`core/store.py`.

**9. Still open from earlier reviews, none regressed:** `compose_filing()`
needs an `rr_number` nothing collects (25 of 31 curated entries); no household
registry, so `segment` must arrive in the payload; no version guard on `Case`
with three writers; no backfill for `FILING#` pointer rows; `.env.example` is
stale and lists `MODEL_SMALL`, `MODEL_MID`, `MODEL_LARGE` and `TAU`, which
nothing reads.

---

## One thing I could not attribute honestly

A `Failed to export span batch code: 400` appears in the runtime logs. I first
claimed it was caused by our own `initialize()` call, from a 0-before/1-after
comparison — **that comparison was against an empty log window and therefore
meaningless.** Tracing works regardless: our `panchayat` log lines carry
`otelTraceID` and `otelTraceSampled: true`. Recorded as unexplained rather
than blamed.

---

## Open PRs

| PR | What | Who should look |
|---|---|---|
| **#29** | the deploy, the Windows `zip` shim, observability findings | anyone |
| **#27** | memstore's stalled queue missing `case_id` tiebreaker | **Kartik** — he raised it |

---

## Suggested split

| Who | Take |
|---|---|
| **Group, 15 min** | the `check_closure` trigger — nothing else unblocks the video's third moment |
| **Ali** | package the Watchdog Lambda, then the trace UI |
| **Frontend** | build against the live ap-south-2 endpoint; `get_case` / `list_cases` / `approve` all work |
| **Kartik** | review #27, then `handlers/ambient.py` |
| **Alakshendra** | routing accuracy number; decide the institution-desk hosting question (#5) |

**The deploy is no longer the constraint. Finishing is.**
