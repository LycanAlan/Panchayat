# Agent stack -- PINNED. Every API path in CLAUDE.md was verified against these
# exact versions on 10 Sep 2026. Four machines drifting to different minors
# mid-sprint is a debugging session nobody has time for. Raise it in the group
# before bumping.
strands-agents[a2a]==1.55.0
strands-agents-tools==0.8.8
bedrock-agentcore==1.22.0
aws-opentelemetry-distro==0.19.0

# AWS
boto3>=1.43.90

# Scoring: brute-force cosine over a few hundred claims.
# Do NOT add opensearch, faiss or pgvector -- see the rejected-alternatives note.
numpy>=2.5.3

# Config + data
pyyaml>=6.0.3
python-dotenv>=1.2.3

# Dev
pytest>=9.1.1
# Pinned with ==, not >=. ruff's DEFAULT RULE SET changes between
# releases, so an unbounded lower bound means "ruff check . is clean"
# resolves to a different question on every laptop -- four people each
# reported clean and the merged tree had 42 errors. See ruff.toml.
# Bumping this is a group call, same bar as the rest of this file.
ruff==0.16.6
