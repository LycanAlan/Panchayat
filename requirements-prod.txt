# RUNTIME ONLY. This is what goes in the image.
#
# requirements.txt stays the single file the four of us develop against and is
# unchanged -- it is one of the three files CLAUDE.md says needs all four of us
# explicitly, and this does not need to touch it.
#
# The Dockerfile used to install requirements.txt, which pulled pytest and ruff
# into a container that files complaints against public bodies. The Dockerfile
# said so itself: "KNOWN, and deliberately not fixed here". It is fixed here
# instead, additively.
#
# VERSIONS ARE NOT RESTATED LOOSELY. Every pin below is byte-identical to the
# one in requirements.txt, and tests/test_requirements.py fails if the two ever
# disagree -- two dependency files that drift apart is a worse problem than the
# one this solves, and it is the obvious way for this to go wrong.
#
# Python 3.12+ is required (numpy>=2.5.3). Declared in pyproject.toml.

# Agent stack -- PINNED. Every API path in CLAUDE.md was verified against these
# exact versions on 10 Sep 2026.
strands-agents[a2a]==1.55.0
strands-agents-tools==0.8.8
bedrock-agentcore==1.22.0
aws-opentelemetry-distro==0.19.0

# AWS
boto3>=1.43.90

# Scoring: brute-force cosine over a few hundred claims.
numpy>=2.5.3

# Config + data
pyyaml>=6.0.3
python-dotenv>=1.2.3
